import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Rewards',
}

const RewardsPage = () => {
  return <div className="relative mx-auto flex w-full max-w-xl flex-col lg:w-1/2">Rewards</div>
}

export { RewardsPage }
